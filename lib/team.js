const Team = require("./member");

function Team() {
    this.name = '';
    this.members = [];
}

Team.prototype.addMember = function(member) {  
    // Add the child to the `children` array if we made it this far
    this.member.push(child);
};

module.exports = Team;